<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/myGreen"
        android:paddingTop="25dp">
        <ImageView
            android:id="@+id/iv_back_notification"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginLeft="@dimen/margin_horizontal"
            android:layout_marginTop="@dimen/margin_vertical"
            android:layout_marginBottom="@dimen/margin_vertical"
            android:src="@drawable/ic_notifications_white"/>
        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginLeft="@dimen/margin_horizontal"
            android:layout_marginTop="@dimen/margin_vertical"
            android:layout_marginBottom="@dimen/margin_vertical"
            android:textSize="@dimen/text_size_first"
            android:textColor="@color/color_first"
            android:text="通知"/>
    </LinearLayout>

    <android.support.design.widget.TabLayout
        android:id="@+id/tb_tab_notification"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:tabBackground="@color/myGreen_light"
        app:tabTextColor="@color/color_second"
        app:tabIndicatorColor="@color/color_first"
        app:tabIndicatorHeight="3dp"
        app:tabSelectedTextColor="@color/color_first">

    </android.support.design.widget.TabLayout>
    <android.support.v4.view.ViewPager
        android:id="@+id/vp_content_notification"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    </android.support.v4.view.ViewPager>

</LinearLayout>